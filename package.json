{
  "name": "Autoresearch",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "api:dev": "uv run --project apps/api uvicorn autoresearch_api.main:app --app-dir apps/api/src --reload --host 0.0.0.0 --port 8000",
    "api:check": "uv run --project apps/api python -m compileall apps/api/src",
    "db:supabase:start": "npx supabase start",
    "infra:up": "docker compose up -d neo4j redis minio minio-init",
    "db:validate": "python tools/validate_database_foundation.py"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.9.14",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
