{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run --project . uvicorn autoresearch_api.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "uv run --project . python -m compileall src",
    "lint": "uv run --project . ruff check src",
    "check-types": "uv run --project . python -m compileall src"
  }
}
