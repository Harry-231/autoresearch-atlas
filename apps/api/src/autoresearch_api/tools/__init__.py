"""The MCP/REST tool boundary: parameterized, read-mostly access to the DAG and
domain context.

Import submodules directly (``tools.service``, ``tools.router``, ``tools.queries``)
to avoid an import cycle with ``dependencies``.
"""
